CREATE TABLE [dbo].[TD_USER] (
    [USR_ID]     INT IDENTITY (1, 1) NOT NULL,
    [USR_LNG_ID] INT NOT NULL,
    CONSTRAINT [PK_TD_USER] PRIMARY KEY CLUSTERED ([USR_ID] ASC),
    CONSTRAINT [FK_TD_USER_TS_LANGUAGE] FOREIGN KEY ([USR_LNG_ID]) REFERENCES [dbo].[TS_LANGUAGE] ([LNG_ID])
);


GO
CREATE NONCLUSTERED INDEX [IX_USR_LNG_ID]
    ON [dbo].[TD_USER]([USR_LNG_ID] ASC);

