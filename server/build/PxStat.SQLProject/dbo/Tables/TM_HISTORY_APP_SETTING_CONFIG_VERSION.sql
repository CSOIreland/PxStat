CREATE TABLE [dbo].[TM_HISTORY_APP_SETTING_CONFIG_VERSION] (
    [HSV_ID]         INT             IDENTITY (1, 1) NOT NULL,
    [HSV_VERSION]    DECIMAL (10, 2) NOT NULL,
    [HSV_CST_ID]     INT             NOT NULL,
    [HSV_UPDATED_AT] DATETIME        DEFAULT (getdate()) NOT NULL,
    [HSV_UPDATED_BY] VARCHAR (256)   DEFAULT (user_name()) NOT NULL,
    [HSV_ASV_ID]     INT             NOT NULL,
    PRIMARY KEY CLUSTERED ([HSV_ID] ASC),
    CONSTRAINT [FK_TM_HISTORY_APP_SETTING_CONFIG_VERSION_TM_APP_SETTING_CONFIG_VERSION] FOREIGN KEY ([HSV_ASV_ID]) REFERENCES [dbo].[TM_APP_SETTING_CONFIG_VERSION] ([ASV_ID]),
    CONSTRAINT [FK_TM_HISTORY_APP_SETTING_CONFIG_VERSION_TS_CONFIG_SETTING_TYPE] FOREIGN KEY ([HSV_CST_ID]) REFERENCES [dbo].[TS_CONFIG_SETTING_TYPE] ([CST_ID])
);

