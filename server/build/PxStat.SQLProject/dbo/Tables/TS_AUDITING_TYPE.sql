CREATE TABLE [dbo].[TS_AUDITING_TYPE] (
    [DTP_ID]    INT           IDENTITY (1, 1) NOT NULL,
    [DTP_CODE]  VARCHAR (32)  NOT NULL,
    [DTP_VALUE] VARCHAR (256) NOT NULL,
    CONSTRAINT [PK_TS_AUDITING_TYPE] PRIMARY KEY CLUSTERED ([DTP_ID] ASC)
);

